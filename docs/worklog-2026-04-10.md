# Work Log — 2026-04-10
**Project:** Chimera Sports Trading Portal (CST)
**GCP Project:** chimera-v4

---

## What Was Done

### 1. Frontend — Portal v1 (CST repo)
- Initialised git repo at `charles-ascot/cst`
- Pushed React + Vite login portal (v1) to GitHub
- Deployed to Cloudflare Pages at `chimerasportstrading.com`
- Resolved Cloudflare deployment issue: new 2026 dashboard forces Workers if you connect GitHub directly — workaround is "Start from new" to stay in Pages flow
- Custom domain added via Cloudflare Pages → CNAME auto-created with proxy (orange cloud) via CNAME flattening

### 2. Frontend — Portal v2
- Promoted `build/chimera-portal-v2` to repo root (replaced v1 files)
- v2 adds: React Router v6, AuthContext (JWT), ProtectedRoute, Layout shell, and placeholder pages for Strategy, Betting Engine, Operations, Dashboards, Reporting, Accounting
- Removed bogus `{context,components,pages}` directory from v2 src
- Pushed to `charles-ascot/cst` — Cloudflare Pages auto-deployed

### 3. Backend API — cst-api
- Pushed FastAPI auth service to new repo `charles-ascot/cst-api`
- Deployed to Cloud Run: `https://cst-api-lssrjnis3q-nw.a.run.app`
- Single admin account auth via bcrypt + JWT
- Environment variables configured in Cloud Run: `JWT_SECRET`, `ADMIN_EMAIL`, `ADMIN_PASSWORD_HASH`, `ALLOWED_ORIGINS`
- `VITE_API_URL` set in Cloudflare Pages → login now fully wired end-to-end

### 4. Decisions Made
- No mock/placeholder data on the UI — ever
- Cloud Run URLs stay as Google-generated until FSU network is built (custom subdomain `api.chimerasportstrading.com` deferred)
- Multi-user auth deferred — single shared admin account until proper user management is built
- Backend repo kept separate from frontend repo (not inside CST)
- FSUs being converted from Python to Kotlin/Springboot — dashboard wiring must be data-only, not programmatic, so switchover won't break the frontend

### 5. FSU Exploration — Dashboard Planning
Explored endpoints for all four FSUs to plan dashboard wiring:

| FSU | Purpose | URL |
|-----|---------|-----|
| FSU1X | Sports odds aggregation (70+ sports, 40+ bookmakers via The Odds API) | `https://beta-fsu1x-lssrjnis3q-nw.a.run.app` |
| FSU1Y | Horse racing data (racecards, runners, form, results via The Racing API) | `https://beta-fsu1y-lssrjnis3q-nw.a.run.app` |
| FSU4 | Email intelligence ingestion (Gmail → AI tagging via Claude → Firestore) | `https://fsu4-lssrjnis3q-nw.a.run.app` |
| FSU4C | Chat intelligence collection (Google Chat → OCR + classification → Firestore) | `https://fsu4c-lssrjnis3q-nw.a.run.app` |

All four confirmed live and healthy.

**Dashboard → FSU mapping agreed:**
- Dashboards page → summary tiles from all 4 FSUs
- Betting Engine → FSU1X (sports odds) + FSU1Y (racecards/runner odds)
- Strategy → FSU4 (intelligence registry, action items, AI query)
- Operations → FSU4 + FSU4C (ingest queues, spaces registry, system health)
- Reporting → FSU4 + FSU4C metrics
- Accounting → not wired to FSUs

---

## Current Status

| Item | Status |
|------|--------|
| Portal live at chimerasportstrading.com | Done |
| Auth (login → JWT → protected dashboard) | Done |
| Dashboard page shells | Live but placeholder |
| API keys for FSU1X/1Y/4/4C | **Needed before dashboard build** |
| Dashboard pages wired to FSUs | **Next — pending API keys** |
| Multi-user auth | Deferred |
| Custom API subdomain | Deferred — after FSU network |
| FSU conversion to Kotlin/Springboot | In progress (separate work) |

---

## Next Session — Pick Up Here
1. Get API keys for FSU1X (`X-API-Key`), FSU1Y (`X-API-Key`), FSU4 (`X-Chimera-API-Key`), FSU4C (`X-Chimera-API-Key`)
2. Add as env vars in Cloudflare Pages
3. Build Dashboards page (summary tiles — quickest visual win)
4. Build Betting Engine — racing tab (FSU1Y racecards → runner odds)
5. Build Strategy page (FSU4 intelligence feed + action items)
